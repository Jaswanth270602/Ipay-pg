version: '3.8'

services:
  # Application service
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ipay-app
    restart: unless-stopped
    working_dir: /var/www/html
    volumes:
      - ./:/var/www/html
      - ./storage:/var/www/html/storage
    networks:
      - ipay-network
    depends_on:
      - mysql
      - redis

  # Nginx service
  nginx:
    image: nginx:alpine
    container_name: ipay-nginx
    restart: unless-stopped
    ports:
      - "8000:80"
    volumes:
      - ./:/var/www/html
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - ipay-network
    depends_on:
      - app

  # MySQL service
  mysql:
    image: mysql:8.0
    container_name: ipay-mysql
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${DB_DATABASE:-badlicash}
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-secret}
      MYSQL_USER: ${DB_USERNAME:-ipay}
      MYSQL_PASSWORD: ${DB_PASSWORD:-secret}
    volumes:
      - mysql-data:/var/lib/mysql
    ports:
      - "3306:3306"
    networks:
      - ipay-network

  # Redis service
  redis:
    image: redis:alpine
    container_name: ipay-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    networks:
      - ipay-network

  # Queue worker service
  queue-worker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ipay-queue-worker
    restart: unless-stopped
    working_dir: /var/www/html
    command: php artisan queue:work --tries=3
    volumes:
      - ./:/var/www/html
    networks:
      - ipay-network
    depends_on:
      - app
      - redis

  # Scheduler service (for cron jobs)
  scheduler:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ipay-scheduler
    restart: unless-stopped
    working_dir: /var/www/html
    command: /bin/sh -c "while true; do php artisan schedule:run --verbose --no-interaction; sleep 60; done"
    volumes:
      - ./:/var/www/html
    networks:
      - ipay-network
    depends_on:
      - app

networks:
  ipay-network:
    driver: bridge

volumes:
  mysql-data:
    driver: local

